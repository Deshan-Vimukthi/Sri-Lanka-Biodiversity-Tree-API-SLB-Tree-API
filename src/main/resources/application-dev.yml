spring:
  datasource:
    url: jdbc:mysql://localhost:3306/slb_tree_db?useSSL=false&serverTimezone=UTC
    username: Kuveni
    password: Kuveni@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: INFO

jwt:
  secret: devSecretKeyForJWT123
  expiration: 3600000
